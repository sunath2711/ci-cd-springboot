apiVersion: apps/v1
kind: Deployment
metadata:
  name: cicd-springboot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cicd-springboot
  template:
    metadata:
      labels:
        app: cicd-springboot
    spec:
      containers:
        - name: app
          image: sunath27/cicd-springboot:latest
          ports:
            - containerPort: 8080
